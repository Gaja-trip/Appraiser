{
  "name": "appraiser-study-web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build-static.mjs",
    "dev": "node scripts/dev-server.mjs",
    "storage:activate": "node scripts/activate-blob.mjs",
    "storage:dry-run": "node scripts/activate-blob.mjs --dry-run",
    "storage:manifest": "node scripts/generate-storage-manifest.mjs",
    "storage:upload": "node scripts/upload-data-to-blob.mjs"
  },
  "optionalDependencies": {
    "@vercel/blob": "latest"
  }
}
